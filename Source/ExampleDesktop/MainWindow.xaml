<Window x:Class="MainWindow"
				xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
				xmlns:local="clr-namespace:ExampleDesktop"
				mc:Ignorable="d"
				Title="Code Explorer"
				Height="800"
				Width="1000"
				FontSize="24"
				WindowState='Maximized'>
	<Window.Resources>
		<Style TargetType='Button'>
			<Setter Property='Margin'
							Value='20,10' />
			<Setter Property='Padding'
							Value='6' />
			<Setter Property='MinWidth'
							Value='145' />
			<Setter Property='VerticalAlignment'
							Value='Top' />
			<Setter Property='HorizontalAlignment'
							Value='Center' />
		</Style>
		<Style TargetType='RadioButton'>
			<Setter Property='VerticalContentAlignment'
							Value='Center' />
		</Style>
	</Window.Resources>

	<DockPanel x:Name='MainPanel'
						 Background="#FFECF7FF"
						 LastChildFill="True">
		<TabControl>
			<TabItem Header='Short Circuit'>
				<StackPanel>

					<TextBlock Text="AndAlso has same logic as AND, but it will not evaluate the second expression if first expression is false."
										 Margin='20'
										 MaxWidth='500'
										 TextWrapping='Wrap'
										 HorizontalAlignment='Left' />
					<TextBlock Margin='20'
										 MaxWidth='500'
										 TextWrapping='Wrap'
										 HorizontalAlignment='Left'
										 Text='This is useful if the second expression is expensive (for example a slow function call).' />

					<Button x:Name="ButtonH"
									HorizontalAlignment='Left'
									Content="And" />
					<Button x:Name="ButtonJ"
									HorizontalAlignment='Left'
									Content="AndAlso"
									Margin='20,10,0,10' />
					<TextBlock Text='...'
										 x:Name='SlowMessageTextBlock'
										 Margin='20' />
				</StackPanel>
			</TabItem>

		</TabControl>

	</DockPanel>
</Window>